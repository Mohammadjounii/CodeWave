@model CodeWave.Web.Models.ForgotPasswordViewModel
@{
    Layout = null;
}
<!DOCTYPE html>
<html class="dark" lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Forgot Password - CodeWave</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300..700&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" rel="stylesheet" />
    <style>
        .material-symbols-outlined {
            font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
        }
    </style>
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        primary: "#b887e3",
                        "background-light": "#f7f6f8",
                        "background-dark": "#19131f"
                    },
                    fontFamily: {
                        display: "Space Grotesk"
                    }
                }
            }
        };
    </script>
</head>
<body class="bg-background-light dark:bg-background-dark font-display text-slate-800 dark:text-slate-200">
    <div class="min-h-screen flex items-center justify-center p-4">
        <div class="w-full max-w-md">
            <div class="bg-[#111318] border border-[#3b4354] rounded-xl p-8">
                <div class="text-center mb-6">
                    <span class="material-symbols-outlined text-primary text-5xl mb-4">lock_reset</span>
                    <h1 class="text-white text-3xl font-bold mb-2">Forgot Password?</h1>
                    <p class="text-[#9da6b9]">Enter your email address and we'll send you a link to reset your password.</p>
                </div>

                @if (TempData["Error"] != null)
                {
                    <div class="mb-6 p-4 bg-red-900/50 border border-red-700 rounded-lg text-red-300">
                        @TempData["Error"]
                    </div>
                }

                <form asp-controller="Settings" asp-action="ForgotPassword" method="post" class="space-y-6">
                    @Html.AntiForgeryToken()
                    
                    <div>
                        <label asp-for="Email" class="block text-white text-sm font-medium mb-2">Email Address</label>
                        <input asp-for="Email" type="email" class="w-full px-4 py-3 bg-[#1c1f27] border border-[#3b4354] rounded-lg text-white focus:outline-none focus:border-primary" placeholder="Enter your email" />
                        <span asp-validation-for="Email" class="text-red-400 text-sm"></span>
                    </div>

                    <button type="submit" class="w-full bg-primary text-white h-12 px-4 rounded-lg hover:bg-primary/90 font-medium">
                        Send Reset Link
                    </button>
                </form>

                <div class="mt-6 text-center">
                    <a asp-controller="User" asp-action="Login" class="text-primary hover:text-primary/80 text-sm">
                        Back to Login
                    </a>
                </div>
            </div>
        </div>
    </div>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
</body>
</html>

